To Execute the program.

Use: "make" command to run the program, and follow the instructions in it.

The range of numbers is increased to 2*input array size, to avoid repetetions of numbers.

Each sort is in it's respective .cpp file.
Each class has it's own implementation of the sort technique.
"sort.cpp" is the Main class, which handles dispay() and user input acceptance functions. 
